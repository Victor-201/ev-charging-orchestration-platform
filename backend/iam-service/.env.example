# IAM Service — Environment Variables
NODE_ENV=production
PORT=3001

# Database
DB_HOST=postgres-iam
DB_PORT=5432
DB_USER=ev_user
DB_PASSWORD=ev_secret
DB_NAME=ev_iam_db

# JWT RS256 — IAM is the ONLY service that needs PRIVATE_KEY
JWT_PRIVATE_KEY_PATH=/var/run/secrets/jwt_private.pem
# Or inline (escape newlines with \n):
# JWT_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----
JWT_PUBLIC_KEY_PATH=/var/run/secrets/jwt_public.pem
JWT_ISSUER=ev-iam-service
JWT_AUDIENCE=ev-platform
JWT_EXPIRES_IN=900s
JWT_REFRESH_EXPIRES_IN=604800s

# Redis (session revocation store)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# RabbitMQ
RABBITMQ_URL=amqp://ev_user:ev_secret@rabbitmq:5672

# Logging
LOG_LEVEL=info
