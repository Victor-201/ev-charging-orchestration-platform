import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Khởi tạo ứng dụng không có lỗi', (WidgetTester tester) async {
    // Basic widget layout assertions
    expect(true, isTrue);
  });
}
