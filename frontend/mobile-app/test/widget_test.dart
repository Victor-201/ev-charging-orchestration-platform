import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Khởi tạo ứng dụng không có lỗi', (WidgetTester tester) async {
    // Kiểm tra cơ bản — tích hợp thực tế sẽ bổ sung sau
    expect(true, isTrue);
  });
}
